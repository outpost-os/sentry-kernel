[package]
name = "sysgate"
version = "0.1.0"
edition = "2021"

[lib]
path = "lib.rs"
test = false
doc = true

[dependencies]
handles = { path = "../../../uapi/include/uapi/handles" }
systypes = { path = "../../../uapi/include/uapi/types" }
managers_bindings = { path = "managers_bindings" }

[features]
mock = []
