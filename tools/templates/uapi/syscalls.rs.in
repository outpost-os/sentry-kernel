// SPDX-FileCopyrightText: 2023 Ledger SAS
// SPDX-License-Identifier: Apache-2.0

/* WARNING: this is an autogenerated file!!! do not edit!!! */

/* Rust implementation of portable part of Sentry syscall */

{% for syscall in syscalls %}
#[no-mangle]
pub extern "C" fn {{ syscall.name }}(
    {% for arg in syscall.args %}
                           {{ arg.name}} :{{ arg.type }}
    {% endfor %}
    )
    let mut ret: usize;
    {% if syscall.numargs is 1 %}
    let ret = forge_syscall1(syscall.id,
        {% for arg in syscall.args %}
                             {{ arg.name}}
                             // todo handle comma here
        {% endfor %}
        )
);
    ret
}

/* and so on */
